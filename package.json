{
  "name": "retail-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "nx run-many --target=dev --all",
    "build": "nx run-many --target=build --all",
    "lint": "nx run-many --target=lint --all",
    "test": "nx run-many --target=test --all",
    "nx": "nx"
  },
  "devDependencies": {
    "@next/swc-wasm-web": "^16.2.11",
    "nx": "^20.0.0",
    "typescript": "^5.6.3"
  }
}
