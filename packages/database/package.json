{
	"name": "@retail/database",
	"version": "0.1.0",
	"private": true,
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"db:migrate": "env -u DATABASE_URL -u LOCAL_DATABASE_URL npx tsx --env-file=../../.env.staging src/migrate.ts",
		"db:migrate:staging": "env -u DATABASE_URL -u LOCAL_DATABASE_URL npx tsx --env-file=../../.env.staging src/migrate.ts",
		"db:migrate:prod": "env -u DATABASE_URL -u LOCAL_DATABASE_URL npx tsx --env-file=../../.env.production src/migrate.ts",
		"db:seed": "env -u DATABASE_URL -u LOCAL_DATABASE_URL npx tsx --env-file=../../.env.staging src/seed.ts",
		"db:seed:staging": "env -u DATABASE_URL -u LOCAL_DATABASE_URL npx tsx --env-file=../../.env.staging src/seed.ts"
	},
	"dependencies": {
		"pg": "^8.13.1"
	},
	"devDependencies": {
		"@types/pg": "^8.11.10"
	}
}
