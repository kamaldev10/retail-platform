{
	"name": "@retail/database",
	"version": "0.1.0",
	"private": true,
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"db:migrate": "npx tsx --env-file=../../.env src/migrate.ts",
		"db:seed": "npx tsx --env-file=../../.env src/seed.ts"
	},
	"dependencies": {
		"pg": "^8.13.1"
	},
	"devDependencies": {
		"@types/pg": "^8.11.10"
	}
}
